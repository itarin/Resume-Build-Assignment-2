extends layout

block content
  h1= title

  ul
    each val in author_list
      li
        a(href=val.url) #{val.name}
        |  (#{val.date_of_birth} - #{val.date_of_death})

    else
      li There are no authors.
